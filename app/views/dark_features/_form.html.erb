<%= form_for(@dark_feature) do |f| %>
  <% if @dark_feature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dark_feature.errors.count, "error") %> prohibited this dark_feature from being saved:</h2>

      <ul>
      <% @dark_feature.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :key %><br />
    <%= f.text_field :key %>
  </div>
  <div class="field">
    <%= f.label :enabled %><br />
    <%= f.check_box :enabled %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
